import 'package:flutter/material.dart';

class Intructions extends StatelessWidget {
  const Intructions({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Tela de instruções"),
    );
  }
}
